#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/email_verification_app_db
#spring.datasource.username=email_verification_app_user
#spring.datasource.password=Jacinth1_
#spring.jpa.hibernate.ddl-auto=create


server:
  error:
    include-binding-errors: always
    include-message: always

spring:
  datasource:
    password: Jacinth1_
    url: jdbc:mysql://localhost:3306/email_verification_app_db
    username: email_verification_app_user

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: localhost
    port: 1025
    username: hello
    password: hello

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000